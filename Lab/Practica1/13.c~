#include <stdio.h>

int main (void)
{
  int Numero, SumaPares = 0, CantidadPares = 0, MediaImpares = 0, CantidadImpares = 0;
  for (i = 1; i <= 10; i++)
    {
      printf ("Introduce un numero");
      scanf ("%d", Numero);
      if (Numero % 2 == 0)
	OperacionesPares (Numero, &SumaPares, &CantidadPares);
      else
	OperacionesImpares (Numero, &MediaImpares, &CantidadImpares);
    }
}

void OperacionesPares (numero, *nueva
{
  
}

void OperacionesImpares
{

}
